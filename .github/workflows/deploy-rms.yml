name: Build and Deploy RMS

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Docker image tag (e.g. v1.0.0.9)"
        required: true
        type: string

env:
  DOCKER_REPO: ingressgroup
  COMPOSE_FILE: docker-compose.prod.yml
  COMPOSE_SERVICE: rms

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Resolve tag
        id: meta
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            TAG="${{ github.event.inputs.tag }}"
          else
            TAG="${GITHUB_REF_NAME}"
          fi

          if [[ ! "$TAG" =~ ^v ]]; then
            echo "Tag must start with 'v' (got: $TAG)" >&2
            exit 1
          fi

          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "Using tag: $TAG"

      - name: Configure Docker Hub credentials
        run: |
          {
            echo "dockerHubUsername=${{ secrets.DOCKERHUB_USERNAME }}"
            echo "dockerHubPassword=${{ secrets.DOCKERHUB_TOKEN }}"
            echo "dockerRepoUrl=${{ env.DOCKER_REPO }}"
          } >> gradle.properties

      - name: Build and push Docker image
        run: |
          chmod +x scripts/build-and-push.sh
          ./scripts/build-and-push.sh \
            -Command stay-board-rms-build-push \
            -Tag "${{ steps.meta.outputs.tag }}"

      - name: Deploy RMS on server
        uses: appleboy/ssh-action@v1.2.0
        env:
          TAG: ${{ steps.meta.outputs.tag }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
          DOCKER_REPO: ${{ env.DOCKER_REPO }}
          COMPOSE_FILE: ${{ env.COMPOSE_FILE }}
          COMPOSE_SERVICE: ${{ env.COMPOSE_SERVICE }}
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          port: ${{ secrets.DEPLOY_PORT }}
          username: ${{ secrets.DEPLOY_USER }}
          password: ${{ secrets.DEPLOY_PASSWORD }}
          envs: TAG,DEPLOY_PATH,DOCKER_REPO,COMPOSE_FILE,COMPOSE_SERVICE
          script_stop: true
          command_timeout: 10m
          script: |
            set -eu

            TAG="${TAG:?TAG is required}"
            DOCKER_REPO="${DOCKER_REPO:-ingressgroup}"
            COMPOSE_FILE="${COMPOSE_FILE:-docker-compose.prod.yml}"
            COMPOSE_SERVICE="${COMPOSE_SERVICE:-rms}"

            if [ -z "${DEPLOY_PATH:-}" ]; then
              if [ -n "${HOME:-}" ] && [ -d "${HOME}/hotel" ]; then
                DEPLOY_PATH="${HOME}/hotel"
              else
                echo "DEPLOY_PATH secret is not set and ${HOME:-HOME}/hotel does not exist." >&2
                echo "Add GitHub secret DEPLOY_PATH=/root/hotel" >&2
                exit 1
              fi
            fi

            IMAGE="${DOCKER_REPO}/stay-board-rms:${TAG}"
            ENV_FILE="${DEPLOY_PATH}/.env"

            echo "Deploy user: $(whoami)"
            echo "Deploy path: ${DEPLOY_PATH}"
            echo "Image: ${IMAGE}"

            if [ ! -d "$DEPLOY_PATH" ]; then
              echo "Deploy path not found: $DEPLOY_PATH" >&2
              exit 1
            fi

            if [ ! -f "$ENV_FILE" ]; then
              echo ".env not found: $ENV_FILE" >&2
              exit 1
            fi

            if [ ! -f "${DEPLOY_PATH}/${COMPOSE_FILE}" ]; then
              echo "Compose file not found: ${DEPLOY_PATH}/${COMPOSE_FILE}" >&2
              exit 1
            fi

            cd "$DEPLOY_PATH"

            if grep -q '^RMS_IMAGE=' "$ENV_FILE"; then
              sed -i "s|^RMS_IMAGE=.*|RMS_IMAGE=${IMAGE}|" "$ENV_FILE"
            else
              echo "RMS_IMAGE=${IMAGE}" >> "$ENV_FILE"
            fi

            echo "RMS_IMAGE set to ${IMAGE}"

            if docker compose version >/dev/null 2>&1; then
              docker compose -f "$COMPOSE_FILE" pull "$COMPOSE_SERVICE"
              docker compose -f "$COMPOSE_FILE" up -d "$COMPOSE_SERVICE"
              docker compose -f "$COMPOSE_FILE" ps "$COMPOSE_SERVICE"
            elif command -v docker-compose >/dev/null 2>&1; then
              docker-compose -f "$COMPOSE_FILE" pull "$COMPOSE_SERVICE"
              docker-compose -f "$COMPOSE_FILE" up -d "$COMPOSE_SERVICE"
              docker-compose -f "$COMPOSE_FILE" ps "$COMPOSE_SERVICE"
            else
              echo "docker compose is not installed on the server" >&2
              exit 1
            fi

            echo "Deploy finished for ${IMAGE}"
